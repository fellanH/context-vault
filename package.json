{
  "name": "context-vault-monorepo",
  "private": true,
  "version": "2.3.0",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/local",
    "packages/hosted"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "release": "npm publish --workspace packages/local"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
