# Server
PORT=3000

# Sentry (optional — error tracking disabled without this)
# SENTRY_DSN=https://...@o4510908750626816.ingest.us.sentry.io/4510908751020032
AUTH_REQUIRED=true

# Stripe billing (optional — billing disabled without these)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO=price_...

# At-rest encryption (optional — entries stored in plaintext without this)
# When set, all vault entries are encrypted with per-user AES-256-GCM keys.
# VAULT_MASTER_SECRET=your-secret-here

# Stripe checkout redirect URLs (auto-detected on Fly.io)
# STRIPE_SUCCESS_URL=https://your-domain.com/billing/success?session_id={CHECKOUT_SESSION_ID}
# STRIPE_CANCEL_URL=https://your-domain.com/billing/cancel

# Google OAuth (optional — Google login disabled without client ID/secret)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-your-secret
# GOOGLE_REDIRECT_URI is auto-derived from the request origin (localhost works out of the box).
# Only set this to override: GOOGLE_REDIRECT_URI=https://your-domain.com/api/auth/google/callback

# CORS origin (defaults to * for dev, restrict in production)
# CORS_ORIGIN=https://your-app.com

# Data storage (defaults to ~/.context-mcp)
# CONTEXT_MCP_DATA_DIR=/data/context-vault
# CONTEXT_MCP_VAULT_DIR=/data/context-vault/vault

# Cloudflare R2 backup (optional — backups disabled without these)
# R2_ACCOUNT_ID=your-account-id
# R2_ACCESS_KEY_ID=your-access-key
# R2_SECRET_ACCESS_KEY=your-secret-key
# R2_BUCKET_NAME=context-vault-backups
# BACKUP_INTERVAL_HOURS=6
